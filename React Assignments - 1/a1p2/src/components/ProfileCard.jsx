// src/components/ProfileCard.jsx

import React from "react";

function ProfileCard({ name, age, location }) {
  return (
    <div>
      <h3>Name: {name /* complete the missing code */}</h3>
      <p>Age: {age /* complete the missing code */}</p>
      <p>Location: {location /* complete the missing code */}</p>
    </div>
  );
}

export default ProfileCard;
